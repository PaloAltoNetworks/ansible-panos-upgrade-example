[tool.poetry]
name = "upgrade-automation-demo"
version = "0.1.0"
description = ""
authors = ["Adam Baumeister <abaumeister@paloaltonetworks.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
